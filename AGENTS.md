# alsosee/finder

## General Repository Rules

- When creating Linear issues for this repository, assign them to the `AlsoSee` Linear project.
- Use Conventional Commits for commit messages, for example `fix: handle comic issue objects` or `chore: update generated structs`.
- Never skip commit verification. Do not use `git commit --no-verify`; fix the failing check instead.
